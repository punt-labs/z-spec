{
  "$schema": "https://github.com/dyoshikawa/rulesync/releases/latest/download/config-schema.json",

  // Per-target object form: features are declared per tool rather than one
  // shared array, because pi does not support `mcp` at all (its supported-
  // tools matrix leaves that cell blank — no mcp_servers-equivalent surface
  // exists for it), so `mcp` is omitted for `pi` and given only to the three
  // tools that actually project it into a native registration file.
  // `permissions` is given to all four: pi's `defaultTools` enable-list is a
  // structurally different mechanism from the other three's allow/ask/deny,
  // authored via the `pi` override block in .rulesync/permissions.jsonc (see
  // that file's comments and the adoption report for the full caveat).
  //
  // NOTE: when `targets` is an object, the top-level `features` key must be
  // omitted — the config loader rejects declaring both (see
  // .tmp/rulesync-config-guide.md "Per-Target Features").
  "targets": {
    "claudecode": ["rules", "mcp", "permissions"],
    "codexcli": ["rules", "mcp", "permissions"],
    "opencode": ["rules", "mcp", "permissions"],
    "pi": ["rules", "permissions"]
  },
  "outputRoots": ["."],
  "delete": true,
  "verbose": false,
  "silent": false,
  "global": false,
  "simulateCommands": false,
  "simulateSubagents": false,
  "simulateSkills": false,
  "gitignoreTargetsOnly": true
}
